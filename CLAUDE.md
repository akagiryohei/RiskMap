# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

RiskMap は、地図上に「危険な出来事」（キャットコール、ナンパ、キャッチ・勧誘、ぶつかりおじさん、リアリング、不審者など）をピン投稿・閲覧できる Web アプリケーション。ユーザーは危険エリアの通知登録や、GPS に基づく危険区域アラートを受け取ることができる。

**現状: `SRC/` ディレクトリは空であり、実装はまだ着手されていない。** 本リポジトリには設計ドキュメント（`DOC/`）のみが存在する。コードを書き始める際は、以下のドキュメントに定義された仕様・方針に従うこと。

**本プロジェクトはユーザーのC#学習を兼ねている。Claude Codeが実装を丸ごと自動生成することは避け、ユーザー自身がソースコードを書く前提でサポートすること。** 具体的には、プロジェクトの雛形作成もコマンドで自動生成せずVisual Studio上の操作手順を案内する、実装は方針の説明・レビュー・質問対応を中心に行い、まとまった量のコードを代わりに書くのはユーザーから明示的に依頼された場合に限る。

## ドキュメント構成

**2026-08-02時点で、設計ドキュメントは `DOC/Markdown/` 配下にマークダウン化済み、かつ工程順（要件定義→基本設計→進捗管理）のディレクトリに再構成済み。ドキュメントの内容を確認・参照する際は、必ずこちらを読むこと（`DOC/要件定義/`・`DOC/基本設計書/` 直下にはもう `.md` はなく、元の .docx/.xlsx のみが残る）。今後の資料の更新・追記もこの Markdown 版で行う運用になっている（元の .docx/.xlsx は更新の起点にはしない。ユーザーが .docx 側を変更した場合は都度知らされる想定）。**

- `DOC/Markdown/01_要件定義/要件資料.md`（元 `DOC/要件定義/要件資料.docx`） — 機能要件（ログイン、地図閲覧、通知、削除依頼、危険度ランキング、絞り込み検索）の詳細仕様
- `DOC/Markdown/02_基本設計/ソフトウェア仕様書.md`（元 `DOC/基本設計書/ソフトウェア仕様書.docx`） — アーキテクチャ、技術スタック、セキュリティ方針、エラーハンドリング方針、ログ出力方針など（最重要ドキュメント）。**3.1.2〜3.1.3節は他プロジェクト(todojava_db)のテンプレートが未更新のまま残っている旨の注記あり。実装時は該当箇所ではなく要件資料.mdを正とすること。**
- `DOC/Markdown/02_基本設計/テーブル定義書.md`（元 `テーブル定義書.xlsx`） — DB テーブル定義（テーブルごとに整理済み）、ER関連の推定含む
- `DOC/Markdown/02_基本設計/機能一覧.md`, `画面設計書.md`, `画面遷移図.md` — 機能一覧・画面設計（レイアウト要素の箇条書き）・画面遷移（画面キャプチャ画像+Mermaid概略フロー付き）
- `DOC/Markdown/03_進捗管理/ソフトウェア仕様書_WBS.md` — WBS（各章の進捗ステータス。「未着手」の章はソフトウェア仕様書側も未記載/未更新であることが多い）
- 画面キャプチャ・図版は `DOC/Markdown/02_基本設計/assets/画面遷移図/` および `DOC/Markdown/02_基本設計/assets/ソフトウェア仕様書/` に格納（Markdownから相対パス `assets/...` で参照）
- `DOC/DR報告書/` — デザインレビュー報告書テンプレート（未マークダウン化）
- `DOC/メモ内容.txt` — 初期検討メモ（Shift-JIS エンコード。UTF-8 として読むと文字化けするため注意）

元の .docx/.xlsx ファイルは `DOC/要件定義/` および `DOC/基本設計書/` 直下に残っているが、参照用の原本という位置づけ。中身を直接確認する必要がある場合（表計算の再集計など）は zip として展開し、`word/document.xml` や `xl/sharedStrings.xml` 内の `<w:t>` / `<t>` タグからテキストを抽出できる。

## 技術スタック（ソフトウェア仕様書より）

- 開発言語: C# 14 / .NET 10
- GUI フレームワーク: Blazor Server (ASP.NET Core)
- DB: MySQL（Pomelo.EntityFrameworkCore.MySql 経由）。位置情報判定に空間関数 `ST_Contains(polygon, point)` を使用
- 地図: Leaflet.js（`IJSRuntime` 経由で JS Interop）、タイルは OpenStreetMap
- リアルタイム通知: Blazor Server 内蔵の SignalR
- メール送信: SendGrid（.NET SDK）
- DB接続リトライ: Polly（最大3回、指数バックオフ 1s→2s→4s）
- ログ: Serilog（ファイルシンク、Docker マウントボリューム上に出力、日次ローテーション・30日保持）
- 実行環境: Docker（`docker-compose.yml` の `volumes:` でログをホスト側 `./logs:/app/logs` にマウントする想定）
- 命名規則: snake_case

## アーキテクチャ上の主要方針

実装時は以下の方針に従うこと（詳細はソフトウェア仕様書 2章参照）。

- **エラーハンドリング**: DB接続失敗は入力欄近傍に赤文字表示、ボタンは非活性化しない。クエリタイムアウト/失敗はダイアログ表示。例外の優先順位は「接続系 MySqlException → タイムアウト/クエリ失敗 → その他 DbException（ErrorBoundary）」の順で評価。
- **ErrorBoundary**: ページコンポーネント単位に配置する（`App.razor` への全体配置は避ける）。書き込み系操作（投稿・登録等）の例外時は入力内容を保持し、「再試行」ボタンで `ErrorBoundary.Recover()` を呼ぶ。
- **ログイン失敗メッセージ**: メールアドレス/パスワードのどちらが誤っているか特定できないよう統一メッセージのみ表示する（アカウント列挙対策）。
- **個人情報保護**: `User` エンティティを直接バインドせず、`PinViewModel` 等の ViewModel に変換してから画面に渡す。メールアドレス等はログ出力時にマスキングする（先頭2文字＋`**@`）。
- **削除依頼の扱い**: 論理削除のみ（`is_deleted`/`delete_flg` + `deleted_at`）、物理削除は行わない。却下3件以上で `users.is_banned` を立て、以降の削除依頼をUI・APIの両方で拒否する。
- **GPS/位置情報**: 取得目的は危険区域判定のみに限定。DB保存座標は小数点以下4桁に丸める。判定に使った座標そのものはログに出さず、判定結果のみ記録する。GPS拒否/タイムアウト時はアラート通知のみ停止し、他機能は継続。
- **SignalR**: 想定同時接続数は最大100（Blazor Server は1接続あたり約250KB消費、コンテナメモリ最低512MB）。100接続超では Azure SignalR Service か Redis Backplane を検討。
- **バリデーション**: フロントエンド・バックエンド双方で実施し、クライアント入力は信頼しない。

## DB テーブル（テーブル定義書より、主要テーブル）

`users`, `secret_tips`（秘密の質問/パスワードヒント）, `category_master`, `post`, `comment`, `area`, `prefecture`, `city`, `post_delete_request`, `comment_delete_request`

## 主要機能一覧

1. ログイン機能（ログイン／パスワード再設定／新規アカウント登録）
2. 地図閲覧機能（ピン表示／ピン投稿／コメント）
3. 通知機能（エリア通知／GPSアラート）
4. 削除・通報依頼機能
5. 危険度ランキング機能（全国／地域別）
6. 絞り込み検索機能（エリア／カテゴリ／時期・時間）
